# Replace with the actual WS endpoint (testnet) once confirmed\ nWS_URL=wss://<lighter-testnet-ws-endpoint>
# If auth is required for private streams, set keys here (placeholders)
API_KEY=
API_SECRET=
WS_URL=wss://mainnet.zklighter.elliot.ai/stream
